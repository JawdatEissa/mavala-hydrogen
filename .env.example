# =============================================================================
# Mavala Hydrogen — Environment Variables Template
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local or any file containing real secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# Shopify Storefront API
# Used by: frontend app to query products, collections, cart, checkout
# How to get: Shopify Admin → Settings → Apps → Develop apps →
#             Create app → Configure Storefront API scopes → Install
# -----------------------------------------------------------------------------
PUBLIC_STOREFRONT_API_TOKEN=your-public-storefront-token-here
PRIVATE_STOREFRONT_API_TOKEN=your-private-storefront-token-here
PUBLIC_STORE_DOMAIN=your-store.myshopify.com
PUBLIC_STOREFRONT_API_VERSION=2025-01

# -----------------------------------------------------------------------------
# Shopify Admin API (for Cursor MCP + server-side admin operations)
# Used by: Cursor MCP so the AI can query/manage the store
# How to get: Same custom app above → Configure Admin API scopes → Install
#             → Reveal Admin API access token (shown ONCE)
# Scopes needed: read_products, write_products, read_orders, read_customers
# NOTE: This token is very sensitive — never commit it
# -----------------------------------------------------------------------------
SHOPIFY_ADMIN_API_TOKEN=shpat_your-admin-token-here
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# -----------------------------------------------------------------------------
# Shopify Customer Account API (for customer login / account pages)
# How to get: Shopify Admin → Settings → Customer accounts
# -----------------------------------------------------------------------------
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=your-customer-account-client-id
PUBLIC_CUSTOMER_ACCOUNT_API_URL=https://shopify.com/your-shop-id/account

# -----------------------------------------------------------------------------
# OpenAI (AI chatbot — already working)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-proj-...

# -----------------------------------------------------------------------------
# Supabase (AI chatbot vector store — already working)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE=your-service-role-key-here

# -----------------------------------------------------------------------------
# Vercel (auto-injected by Vercel CLI — do not set manually)
# -----------------------------------------------------------------------------
# VERCEL_OIDC_TOKEN=auto-injected
