# Dependencies
node_modules

# Build outputs
build
.cache
dist
.vercel

# Environment secrets — NEVER commit any .env files with real values
.env
.env.*
!.env.example

# Cursor MCP config — may contain Shopify Admin API token or other secrets
.cursor/mcp.json

# Local hero video rips / inspection — often >100MB (GitHub block)
downloads/hero-videos-inspect/
